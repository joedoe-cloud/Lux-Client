pluginManagement {
    repositories {
        maven {
            name = "Fabric"
            url = "https://maven.fabricmc.net/"
        }

        maven {
            name = "Maven Central"
            url = "https://repo.maven.apache.org/maven2/"
        }

        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        maven {
            name = "Fabric"
            url = "https://maven.fabricmc.net/"
        }

        mavenCentral()
    }
}

rootProject.name = "LuxClient"
